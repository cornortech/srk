# ============================================
# SRK Backend Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# IMPORTANT: Never commit .env file to version control

# --------------------------------------------
# Runtime / Environment
# --------------------------------------------
# development | production
NODE_ENV=development

# Backend port
PORT=4000

# Controls the R2 upload namespace.
# srk -> uploads go under srk/
# anything else -> uploads go under dev/
IS_PROD=false

# --------------------------------------------
# Database Configuration
# --------------------------------------------
# MongoDB connection string
# Format: mongodb://localhost:27017/database_name
# Or Atlas: mongodb+srv://username:password@cluster.mongodb.net/database_name
DATABASE_URL=

# --------------------------------------------
# URLs and Auth
# --------------------------------------------
# Frontend application base URL (for email links and redirects)
FRONTEND_BASE_URL=

# Secondary frontend URLs used by backend flows
TASK_FRONTEND_URL=http://localhost:4400
GROW_FRONTEND_URL=http://localhost:4500
BANK_FRONTEND_URL=http://localhost:4300

# Optional cookie domain for auth flows
COOKIE_DOMAIN=

# Secret key for JWT token encryption
# Generate a random 64-character string: openssl rand -hex 32
JWT_SECRET=

# Comma-separated list of allowed CORS origins
WHITE_LISTED_ORIGINS=http://localhost:3000,http://localhost:4000,http://localhost:4300,http://localhost:4400,http://localhost:4500,http://localhost:5000,http://localhost:5173,http://localhost:5174,http://localhost:4200

# --------------------------------------------
# Email Configuration (SMTP)
# --------------------------------------------
# Email address used to send system emails (registration, notifications, etc.)
APP_EMAIL=

# SMTP password for the email account
# For Gmail: Use App Password (not your regular password)
# Enable 2FA and generate app password at: https://myaccount.google.com/apppasswords
SMTP_PW=

# --------------------------------------------
# Cloudflare R2 Configuration
# --------------------------------------------
# Uploads use the prefix derived from PROD: srk/ or dev/
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ENDPOINT=https://5f09c9e5753d5a473d39fed1135fef46.r2.cloudflarestorage.com
R2_BUCKET=srk
CDN_BASE_URL=https://cdn.thesrkuniversity.com

# --------------------------------------------
# Firebase Admin SDK Configuration
# --------------------------------------------
# Firebase project ID from Firebase Console
# Get from: Firebase Console > Project Settings > General


# --------------------------------------------
# Email Configuration (SMTP)
# --------------------------------------------
# Email address used to send system emails (registration, notifications, etc.)
APP_EMAIL=

# SMTP password for the email account
# For Gmail: Use App Password (not your regular password)
# Enable 2FA and generate app password at: https://myaccount.google.com/apppasswords
SMTP_PW=

# --------------------------------------------
# Application URLs
# --------------------------------------------
# Frontend application base URL (for email links and redirects)
# Development: http://localhost:4200
# Production: https://yourdomain.com
FRONTEND_BASE_URL=

# --------------------------------------------
# Security Configuration
# --------------------------------------------
# Secret key for JWT token encryption
# Generate a random 64-character string: openssl rand -hex 32
JWT_SECRET=

# Comma-separated list of allowed CORS origins
# Add your frontend URLs here to allow API access
WHITE_LISTED_ORIGINS=http://localhost:3000,http://localhost:4000,http://localhost:5000,http://localhost:5173,http://localhost:5174,http://localhost:4200

# --------------------------------------------
# Firebase Admin SDK Configuration
# --------------------------------------------
# Firebase project ID from Firebase Console
# Get from: Firebase Console > Project Settings > General
FIREBASE_PROJECT_ID=

# Firebase service account client email
# Get from: Firebase Console > Project Settings > Service Accounts
# Download the service account JSON file and copy these values
FIREBASE_CLIENT_EMAIL=

# Firebase service account private key
# IMPORTANT: Keep the \n characters in the key for proper formatting
# Format: -----BEGIN PRIVATE KEY-----\nYOUR_KEY_HERE\n-----END PRIVATE KEY-----\n
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nMIIEvg